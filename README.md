# Auth0 User Cleanup

Remove users that have not logged in for a given number of days.

* Configure with the management API client id, secret, etc.
* Set `retentionDays`. The default is 30.

```bash
npm install
node index.js
```
